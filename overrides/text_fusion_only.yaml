---
# 仅使用融合，不使用对齐损失
# 这样可以评估纯粹的特征融合效果

# 禁用对齐
use_align: false
alignment_weight: 0.0

# 使用交叉网络进行更好的融合
use_cross: true
text_cross_layer_num: 3

# 适中的文本权重
text_weight: 0.5
text_gate_init: 0.5

# 可选：添加一些正则化防止过拟合
cross_dropout_prob: 0.1
text_gate_reg_l2: 0.001
